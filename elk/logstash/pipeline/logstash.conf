input {
  file {
    path => "/var/log/app/*.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"
  }
  beats {
    port => 5044
  }
}

filter {
  if [message] !~ /^\d{4}-\d{2}-\d{2}/ {
    drop { }
  }

  # Format avec trace_id/span_id (server/client Python)
  grok {
    match => {
      "message" => "%{TIMESTAMP_ISO8601:timestamp} - %{DATA:service} - %{LOGLEVEL:level} - \[trace_id=%{DATA:trace_id} span_id=%{DATA:span_id}\] - %{GREEDYDATA:log_message}"
    }
    tag_on_failure => []
  }

  # Format sans trace_id (logs TP microservices)
  if ![trace_id] {
    grok {
      match => {
        "message" => "%{TIMESTAMP_ISO8601:timestamp} - %{LOGLEVEL:level} - \[%{DATA:service}\] - %{GREEDYDATA:log_message}"
      }
    }
  }

  mutate {
    gsub => ["log_message", "\[[0-9;]*m", ""]
  }

  # Extraire endpoint, code HTTP et latence depuis les logs client
  if [log_message] =~ /Endpoint/ {
    grok {
      match => {
        # Couvre SUCCESS (200 OK), SERVER ERROR (500), CLIENT ERROR (422), etc.
        "log_message" => "%{DATA:http_status_text} \(%{NUMBER:http_code} %{WORD}\) \| Endpoint: %{URIPATH:endpoint} \| Latency: %{NUMBER:latency_seconds:float}s"
      }
      tag_on_failure => []
    }
  }

  # Taguer les logs liés aux opérations PostgreSQL
  if [log_message] =~ /[Pp]ostgre[Ss][Qq][Ll]|database|Database/ {
    mutate {
      add_tag => ["db_operation"]
    }
  }

  # Taguer les erreurs DB spécifiquement (utile pour les alertes Kibana)
  if [log_message] =~ /[Ee]rror|[Ff]ailed|[Tt]imeout/ and "db_operation" in [tags] {
    mutate {
      add_tag => ["db_error"]
      add_field => { "db_error" => "true" }
    }
  }

  date {
    match => ["timestamp", "yyyy-MM-dd HH:mm:ss,SSS"]
    target => "@timestamp"
  }

  mutate {
    remove_field => ["timestamp", "message", "agent", "ecs", "input"]
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "ynov-logs-%{+YYYY.MM.dd}"
  }
}